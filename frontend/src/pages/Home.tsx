// Layout skeleton only. Components below are wired with real state
// and API calls in later phases (Phase 5).

function Home() {
  return (
    <main className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-2xl font-semibold text-gray-800">
        NL to SQL Generator
      </h1>
      {/* TODO (Phase 5): FileUpload, SchemaViewer, QueryInput, SQLOutput */}
    </main>
  );
}

export default Home;
