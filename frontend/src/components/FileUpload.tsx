// Will post the selected file to POST /upload and hand the
// returned Schema up to the parent. No logic yet (Phase 5).

function FileUpload() {
  return <div>{/* TODO (Phase 5): file input + upload handler */}</div>;
}

export default FileUpload;
