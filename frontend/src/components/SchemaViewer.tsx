// Read-only display of the extracted/manual Schema so the user can
// verify it before generating SQL. No logic yet (Phase 5).

function SchemaViewer() {
  return <div>{/* TODO (Phase 5): render tables/columns from Schema */}</div>;
}

export default SchemaViewer;
