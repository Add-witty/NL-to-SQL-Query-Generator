// Mirrors app/models/schema_models.py on the backend.
// TODO (Phase 3): define Column, Table, Schema interfaces once the
// backend Schema model is finalized, to keep both sides in sync.

export {};
